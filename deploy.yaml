apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: chatroom
  name: chatroom
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chatroom
  strategy: {}
  template:
    metadata:
      labels:
        app: chatroom
    spec:
      containers:
        - image: us-east1-docker.pkg.dev/personal-project-429201/my-repo/studyroom:1cbc05d9
          name: nginx
