apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert
spec:
  secretName: tls-cert
  dnsNames:
    - studyroom.ajayproject.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
