apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: chatroom
  name: chatroom
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chatroom
  strategy: {}
  template:
    metadata:
      labels:
        app: chatroom
    spec:
      containers:
        - image: us-east1-docker.pkg.dev/personal-project-429201/my-repo/studyroom:fd5d7f2a
          name: nginx
